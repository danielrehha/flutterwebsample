import 'package:flutter/material.dart';

const EdgeInsets defaultPadding =
    EdgeInsets.symmetric(horizontal: 25, vertical: 25);
const EdgeInsets defaultPaddingNarrow =
    EdgeInsets.symmetric(horizontal: 50, vertical: 25);
const EdgeInsets widthPaddingNarrow =
    EdgeInsets.symmetric(horizontal: 50, vertical: 0);
const EdgeInsets widthPaddingMedium =
    EdgeInsets.symmetric(horizontal: 25, vertical: 0);
const EdgeInsets heightPaddingNarrow =
    EdgeInsets.symmetric(horizontal: 0, vertical: 25);
const EdgeInsets defaultPaddingWide =
    EdgeInsets.symmetric(horizontal: 10, vertical: 10);
const EdgeInsets defaultPaddingMedium =
    EdgeInsets.symmetric(horizontal: 25, vertical: 25);

const SizedBox DefaultVerticalSpacer = SizedBox(height: 20);

const SizedBox DefaultHorizontalSpacer = SizedBox(width: 10);

const double itemListVerticalSpacing = 8;

const double paddingBelowHeader = 10;

const double defaultColumnWidth = 500;

const EdgeInsets applicationListWidgetPadding =
    const EdgeInsets.only(top: 6, bottom: 6, right: 12, left: 12);
