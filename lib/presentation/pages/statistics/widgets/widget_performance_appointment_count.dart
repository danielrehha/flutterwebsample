import 'package:flutter/material.dart';

class AppointmentCountPerformanceWidget extends StatelessWidget {
  const AppointmentCountPerformanceWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}