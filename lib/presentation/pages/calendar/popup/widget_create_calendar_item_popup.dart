import 'package:flutter/material.dart';

class CreateCalendarItemPopup extends StatelessWidget {
  const CreateCalendarItemPopup({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}